# Telegram-CIDR-Regions
每日更新的 Telegram CIDR 分流规则，通过权威ASN数据接口拉取各ASN下CIDR列表，并按地区进行归类合并去重。 Telegram CIDR routing rules are updated daily. The CIDR list for each ASN is pulled through an authoritative ASN data interface, and duplicates are cleaned and merged by region.
